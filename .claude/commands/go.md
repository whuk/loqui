Read plan.md and find the next unmarked test (a test without a checkmark).

1. Implement ONLY that single test
2. Run the test to confirm it fails (Red phase)
3. Implement the MINIMUM code to make the test pass
4. Run all tests to confirm they pass (Green phase)
5. Mark the test as complete in plan.md with a checkmark

Do NOT:
- Implement multiple tests at once
- Add code beyond what's needed to pass the test
- Refactor during this phase
- Skip the failing test verification

Report which test you implemented and the test results.
