Run all tests and report the results.

1. Execute the test suite
2. Report:
   - Total tests run
   - Passed/Failed count
   - Any failing test details with clear error messages
   - Test execution time

If tests fail, do NOT attempt to fix them automatically. Just report the failures clearly so the TDD cycle can be followed properly.

$ARGUMENTS - Optional: specific test file or pattern to run